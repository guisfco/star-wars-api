access:
  getPlanets: "https://swapi.dev/api/planets/"

spring:
  data:
    mongodb:
      host: ${DATABASE_HOST}
      port: ${DATABASE_PORT}
      database: starwars

server:
  servlet:
    context-path: /api
  error:
    include-message: always
  port: 8080